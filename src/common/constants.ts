export const API_VERSION = "/api/v1";
export const enum HTTP_STATUS {
    ok = 200,
    created = 201,
    badRequest = 400,
    unauthorized = 401,
    notFound = 404,
    internalServerError = 500,
}
